"Scoundrel-Solitaire"
Current version 0.4.9
(comment inside main might suggest otherwise so ignore it)\n
Current build has most functionality implemented. Next build will include instructions. Soon to be updated. Next version will probably be pushed to main.